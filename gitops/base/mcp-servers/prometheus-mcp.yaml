# Prometheus MCP Server
apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-mcp-config
  namespace: k2a-mcp-servers
  labels:
    app.kubernetes.io/name: prometheus-mcp
    app.kubernetes.io/part-of: k2a-enterprise-monitoring
data:
  PROMETHEUS_URL: "https://thanos-querier.openshift-monitoring.svc.cluster.local:9091"
  PROMETHEUS_VERIFY_SSL: "false"
  LOG_LEVEL: "info"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus-mcp
  namespace: k2a-mcp-servers
  labels:
    app.kubernetes.io/name: prometheus-mcp
    app.kubernetes.io/part-of: k2a-enterprise-monitoring
    app.kubernetes.io/component: mcp-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: prometheus-mcp
  template:
    metadata:
      labels:
        app.kubernetes.io/name: prometheus-mcp
        app.kubernetes.io/part-of: k2a-enterprise-monitoring
    spec:
      serviceAccountName: k2a-monitoring
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: prometheus-mcp
          image: image-registry.openshift-image-registry.svc:5000/k2a-mcp-servers/prometheus-mcp:latest
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsNonRoot: true
          ports:
            - containerPort: 8000
              name: http
              protocol: TCP
          command:
            - python
            - -m
            - prometheus_mcp
          args:
            - --transport
            - streamable-http
            - --port
            - "8000"
          envFrom:
            - configMapRef:
                name: prometheus-mcp-config
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: prometheus-mcp
  namespace: k2a-mcp-servers
  labels:
    app.kubernetes.io/name: prometheus-mcp
    app.kubernetes.io/part-of: k2a-enterprise-monitoring
spec:
  selector:
    app.kubernetes.io/name: prometheus-mcp
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  type: ClusterIP
