apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - prometheus-mcp.yaml
  - alertmanager-mcp.yaml
  - redhat-cases-mcp.yaml

commonLabels:
  app.kubernetes.io/part-of: k2a-enterprise-monitoring
  app.kubernetes.io/managed-by: argocd
