# AlertManager MCP Server Deployment for OpenShift
# Custom K2A implementation
apiVersion: v1
kind: Secret
metadata:
  name: alertmanager-mcp-secrets
  namespace: k2a-mcp-servers
  labels:
    app.kubernetes.io/name: alertmanager-mcp
    app.kubernetes.io/part-of: k2a-enterprise-monitoring
type: Opaque
stringData:
  # Bearer token for AlertManager authentication (if required)
  ALERTMANAGER_TOKEN: ""
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: alertmanager-mcp-config
  namespace: k2a-mcp-servers
  labels:
    app.kubernetes.io/name: alertmanager-mcp
    app.kubernetes.io/part-of: k2a-enterprise-monitoring
data:
  ALERTMANAGER_URL: "http://alertmanager-main.openshift-monitoring.svc.cluster.local:9093"
  LOG_LEVEL: "info"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: alertmanager-mcp
  namespace: k2a-mcp-servers
  labels:
    app.kubernetes.io/name: alertmanager-mcp
    app.kubernetes.io/part-of: k2a-enterprise-monitoring
    app.kubernetes.io/component: mcp-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: alertmanager-mcp
  template:
    metadata:
      labels:
        app.kubernetes.io/name: alertmanager-mcp
        app.kubernetes.io/part-of: k2a-enterprise-monitoring
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: alertmanager-mcp
          # TODO: Update with your container registry
          image: image-registry.openshift-image-registry.svc:5000/k2a-mcp-servers/alertmanager-mcp:latest
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsNonRoot: true
          ports:
            - containerPort: 8000
              name: http
              protocol: TCP
          command:
            - python
            - -m
            - alertmanager_mcp
          args:
            - --transport
            - streamable-http
            - --port
            - "8000"
          envFrom:
            - configMapRef:
                name: alertmanager-mcp-config
            - secretRef:
                name: alertmanager-mcp-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: alertmanager-mcp
  namespace: k2a-mcp-servers
  labels:
    app.kubernetes.io/name: alertmanager-mcp
    app.kubernetes.io/part-of: k2a-enterprise-monitoring
spec:
  selector:
    app.kubernetes.io/name: alertmanager-mcp
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  type: ClusterIP
---
# BuildConfig para construir la imagen en OpenShift
apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: alertmanager-mcp
  namespace: k2a-mcp-servers
  labels:
    app.kubernetes.io/name: alertmanager-mcp
    app.kubernetes.io/part-of: k2a-enterprise-monitoring
spec:
  source:
    type: Git
    git:
      uri: https://github.com/YOUR_ORG/k2a-enterprise-monitoring.git
      ref: main
    contextDir: mcp-servers/alertmanager-mcp
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile
  output:
    to:
      kind: ImageStreamTag
      name: alertmanager-mcp:latest
  triggers:
    - type: ConfigChange
---
apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: alertmanager-mcp
  namespace: k2a-mcp-servers
  labels:
    app.kubernetes.io/name: alertmanager-mcp
    app.kubernetes.io/part-of: k2a-enterprise-monitoring
